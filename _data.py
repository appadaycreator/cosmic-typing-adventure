LEGACY_HTML = True  # 既存HTMLを保持（再アセンブル禁止）
TITLE = 'タイピング練習【無料】宇宙テーマで楽しく速度アップ！'
DESCRIPTION = 'タイピング速度（WPM）を測定しながら宇宙探索ゲームで楽しく練習。プログラミング学習・在宅ワーク・事務職に必須のタイピング力を無料でスキルアップ。苦手キー克服機能付き。'
JS_CODE = '// Global variables\n        let currentLanguage = \'ja\';\n        let currentFontSize = \'base\';\n\n        // Initialize page\n        document.addEventListener(\'DOMContentLoaded\', function () {\n            initializeAnimations();\n            initializeProgressChart();\n            setupServiceWorker();\n            loadUserPreferences();\n\n            console.log(\'🚀 Cosmic Typing Adventure initialized\');\n            console.log(\'Language:\', currentLanguage);\n            console.log(\'Font Size:\', currentFontSize);\n        });\n\n        // Language toggle\n        function toggleLanguage(lang) {\n            currentLanguage = lang;\n            localStorage.setItem(\'preferred_language\', lang);\n\n            // Update active language button\n            document.querySelectorAll(\'[id^="lang-"]\').forEach(btn => {\n                btn.classList.remove(\'text-cyan-400\');\n                btn.classList.add(\'text-gray-400\');\n            });\n            document.getElementById(`lang-${lang}`).classList.add(\'text-cyan-400\');\n\n            // Here you would implement actual language switching\n            console.log(`Language switched to: ${lang}`);\n\n            // Show notification\n            showNotification(`言語を${lang === \'ja\' ? \'日本語\' : \'English\'}に変更しました`, \'success\');\n        }\n\n        // Font size adjustment\n        function changeFontSize(size) {\n            currentFontSize = size;\n            localStorage.setItem(\'preferred_font_size\', size);\n\n            // Remove all font size classes\n            document.body.classList.remove(\'font-size-xs\', \'font-size-sm\', \'font-size-base\', \'font-size-lg\', \'font-size-xl\');\n\n            // Add new font size class\n            document.body.classList.add(`font-size-${size}`);\n\n            console.log(`Font size changed to: ${size}`);\n            showNotification(`文字サイズを変更しました`, \'success\');\n        }\n\n        // Mobile menu toggle\n        function toggleMobileMenu() {\n            const menu = document.getElementById(\'mobile-menu\');\n            menu.classList.toggle(\'active\');\n        }\n\n        // Smooth scroll to section\n        function scrollToSection(sectionId) {\n            const element = document.getElementById(sectionId);\n            if (element) {\n                element.scrollIntoView({ behavior: \'smooth\' });\n            }\n        }\n\n        // Initialize animations\n        function initializeAnimations() {\n            // Demo stats animation\n            animateValue(\'demo-wpm\', 0, 85, 2000);\n            animateValue(\'demo-accuracy\', 0, 94, 2000, \'%\');\n            animateValue(\'demo-planets\', 0, 23, 2000);\n            animateValue(\'demo-level\', 1, 12, 2000);\n\n            // Floating animations for features\n            const features = document.querySelectorAll(\'.floating-animation\');\n            features.forEach((feature, index) => {\n                feature.style.animationDelay = `${index * 0.5}s`;\n            });\n        }\n\n        // Animate number values\n        function animateValue(elementId, start, end, duration, suffix = \'\') {\n            const element = document.getElementById(elementId);\n            if (!element) return;\n\n            const range = end - start;\n            const increment = range / (duration / 16);\n            let current = start;\n\n            const timer = setInterval(() => {\n                current += increment;\n                if (current >= end) {\n                    element.textContent = end + suffix;\n                    clearInterval(timer);\n                } else {\n                    element.textContent = Math.floor(current) + suffix;\n                }\n            }, 16);\n        }\n\n        // Initialize progress chart\n        function initializeProgressChart() {\n            const ctx = document.getElementById(\'progressChart\');\n            if (!ctx) return;\n\n            new Chart(ctx, {\n                type: \'line\',\n                data: {\n                    labels: [\'1日目\', \'7日目\', \'14日目\', \'21日目\', \'28日目\'],\n                    datasets: [{\n                        label: \''
MAIN_HTML = '<div><button class="btn">開始する</button></div>'
FAQ = [
    ('タイピング練習は無料で使えますか？', 'はい、完全無料・登録不要でご利用いただけます。'),
    ('スマートフォンで使えますか？', 'はい、スマートフォン・タブレット・PCすべてでご利用いただけます。'),
    ('途中で一時停止できますか？', 'はい、一時停止ボタンで止めて、再開することができます。'),
    ('カスタムの時間を設定できますか？', 'はい、プリセット以外に任意の時間を直接入力して設定できます。'),
    ('時間終了時に通知はありますか？', '画面上でアラート表示します。ブラウザの設定によって音声が出る場合があります。'),
]
HOW_TO = [
    '時間をプリセットまたは手入力で設定する',
    '必要に応じてオプション設定を行う',
    'スタートボタンをクリックして開始する',
    '残り時間を確認しながら作業・発表を進める',
    '一時停止・リセットボタンで時間を管理する',
]
FOOTER_LINKS = [('https://appadaycreator.com/sleep-quality-checker/', '睡眠の質チェッカー'), ('https://appadaycreator.com/bmi-body-tracker/', 'BMI・体重管理'), ('https://appadaycreator.com/household-budget-analyzer/', '家計簿診断')]
